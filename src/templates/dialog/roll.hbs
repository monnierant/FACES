<div class="faces-dialog-content">
  <h2>{{ talent.name }} ({{ talent.value }})</h2>
  <section class="faces-dialog-modifiers">
    <div class="faces-dialog-modifier">
      <label
        for="faces-dialog-modifier-difficulty"
        >{{ localize 'MYSYST.Global.difficulty' }}</label
      >
      <select id="faces-dialog-modifier-difficulty" name="difficulty">
        {{#each difficultyLevels as |difficulty|}}
        <option value="{{ difficulty.value }}">
          {{ localize (concat 'MYSYST.Dialog.difficulty.' difficulty.label) }}
          ({{ difficulty.value }})
        </option>
        {{/each}}
      </select>
    </div>
  </section>
</div>
<div class="dialog-buttons">
  {{#each buttons as |button id|}}

  <button
    class="dialog-button {{ cssClass }}"
    data-button="{{ id }}"
    {{disabled
    button.disabled}}
  >
    {{{button.icon}}} {{{button.label}}}
  </button>
  {{/each}}
</div>
